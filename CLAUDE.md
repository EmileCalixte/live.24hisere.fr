# Instructions

- Read `README.md` first before any exploration or action in this repo.
- Always use the scripts defined in `package.json` files instead of calling tools directly via `npx` or equivalent. Prefer the root `package.json` scripts (e.g. `pnpm test:e2e`) over individual app/package scripts whenever available.
