import { WINDOW_WIDTH_BREAKPOINTS } from "./sizing";

/**
 * If the window width is less than this value,
 * the main menu must switch to responsive mode
 */
export const RESPONSIVE_MENU_BREAKPOINT_PX = WINDOW_WIDTH_BREAKPOINTS.LG;
