meta {
  name: Obtain access token
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/login
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  username: admin
  password: admin
}

vars:post-response {
  accessToken: res.body.accessToken
}
